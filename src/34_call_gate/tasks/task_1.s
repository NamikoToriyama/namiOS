task_1:
		;---------------------------------------
		; 文字列の表示
		;---------------------------------------
		cdecl	SS_GATE_0:0, 63, 0, 0x07, .s0		; draw_str();
.10L:
		;---------------------------------------
		; 時刻の表示
		;---------------------------------------
		; これはコールゲートに書かれてないのでコメントアウトする
		;mov		eax, [RTC_TIME]					; 時刻の取得
		;cdecl	draw_time, 72, 0, 0x0700, eax	; 時刻の表示

		;---------------------------------------
		; タスクの呼び出し
		;---------------------------------------
		jmp		.10L

		;---------------------------------------
		; データ
		;---------------------------------------
.s0		db	"Task-WAN", 0
